import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    background: "#0f131a",
    primary: "#2d343a",
    primaryText: "#a4a6a8",
    white: "#ffffff",
    black: "#000000",
  },
});

export default theme;
